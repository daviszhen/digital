services:
  postgres:
    image: postgres:16-alpine
    container_name: dstore-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: payload
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: dstore-redis
    ports:
      - "6380:6379"
    restart: unless-stopped

  app:
    build:
      context: .
      args:
        NEXT_PUBLIC_SERVER_URL: https://localhost:8443
    container_name: dstore-app
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/payload
      PAYLOAD_SECRET: payload-docker-secret-change-me
      SERVER_URL: https://localhost:8443
    env_file:
      - .env.docker
    volumes:
      - media_uploads:/app/public/media
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  caddy:
    image: caddy:alpine
    container_name: dstore-caddy
    ports:
      - "8443:8443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./certs:/certs
    depends_on:
      - app
    restart: unless-stopped

volumes:
  pgdata:
  media_uploads:
